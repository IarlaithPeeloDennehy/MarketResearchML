fastapi==0.111.0
uvicorn[standard]==0.29.0
yfinance>=0.2.50
scikit-learn==1.7.2
pandas==2.3.3
numpy==2.4.4
scipy==1.17.1
httpx==0.27.0
pydantic>=2.7.1
python-multipart==0.0.9
joblib==1.4.2
sqlmodel>=0.0.19
psycopg2-binary>=2.9.9
alembic>=1.13.0
bcrypt>=4.0.0
slowapi>=0.1.9
finnhub-python>=2.4.19
# NOTE: torch is intentionally NOT here. The offline-pretrained encoder is
# opt-in and the app degrades gracefully without torch (price-ranks-only). To
# enable the encoder in production, also install backend/requirements-encoder.txt
# and set USE_ENCODER=1 + ENCODER_URL. Keeping torch out of the base deploy means
# a torch wheel/resolution issue can never break the core service.
